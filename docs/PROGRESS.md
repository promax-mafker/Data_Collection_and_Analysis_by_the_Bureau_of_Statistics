# 项目进度 · 统计局数据采集与分析平台

> 本文件是项目**唯一进度基准**：每次迭代后更新此文件，替代零散聊天记录。
> 最近更新：2026-09-03（查询层宽容匹配交付）

## 1. 一句话定位

一个 Python 系统：从国家统计局出发**逐级自动发现**省/市统计局站点，采集年度统计公报（网页 + PDF 文字），**规则抽取**为结构化指标并按年度分门别类入库，输出 Web 管理与**省/市对比分析报告**。试点：福建省全流程。

## 2. 状态总览

```
需求澄清 → 设计 → 实现 → 测试 → 真实验证 → 交付
   [M0]      [M0]   [M1]   [M1-M4]   [M3-M4]    [M2] ✅
                                                二期(M5) ⏳ 未开始
```

| 里程碑 | 状态 | 关键产出 |
|--------|------|---------|
| M0 需求与设计 | ✅ 完成 | `docs/DESIGN.md`（设计）、`docs/PLAN.md`（实现计划） |
| M1 核心采集平台 | ✅ 完成 | 递进发现 / 采集 / 规则抽取 / SQLite 入库 / Web / CLI |
| M2 GitHub 交付 | ✅ 完成 | 完整 README；经 GitHub MCP 推送到用户仓库（含 contribution 归属） |
| M3 健壮性修复 | ✅ 完成 | 真实数据驱动：早停优化、同域过滤、内容去重、正文清洗、年份修正 |
| M3.1 审查修复 | ✅ 完成 | 独立代码审查（Critical 2 + Important 6 已修，详见 §4.1） |
| M4 福建全流程扩展 | ✅ 完成 | 莆田接入（was5 检索 + PDF 提取）；分析报告模块 |
| M4.1 缺口治理 | ✅ 完成 | 覆盖「零数据」缺口：列表页下钻 / fallback 补漏 / 自洽校验 / listing_urls（详见 §4.2） |
| M4.2 查询宽容层 | ✅ 完成 | 解决「数据在库却查不到」：地区剥前缀/补全、年份容错、指标别名、歧义提示（详见 §4.3） |
| M4.3 整句自然查询 | ✅ 完成 | 一句话同时抽地区/指标/年份，支持多地区并列与「人均」防误配（详见 §4.4） |
| M5 泉州纵深画像 | ✅ 完成 | 政府公开文件全家桶：规划/报告/预决算/企业名录；LLM 抽取+批判审读+数理分析（详见 §4.5） |
| M6 经济驱动画像 | ✅ 完成 | 实际产业结构错配 + 三驾马车(消费/债务投资/出口) + Kami Parchment 报告（详见 §4.6） |
| M7 二期规划 | ⏳ 未开始 | 见 §6 |
| M7a 管线鲁棒性 P0 | ✅ 完成 | 对抗性审查 → P0 修复：单值化/口径/事务/白名单/批判接线/region 隔离/数据清理（详见 §4.7） |

## 3. 当前能力清单（已验证）

**采集**
- 三级统计局 URL 递进发现（国家 → 福建 → 9 设区市），白名单优先 + 命中目标即停。
- 手工锚点机构（`manual_bureaus`）：覆盖无独立 `tjj` 域名的站点（莆田）。
- 公报定位：站点首页关键词链接 / 门户 was5 站内检索（动态列表站点）。
- 内容源：网页 HTML + **PDF 文字**（pdfplumber）；同域过滤 + 内容哈希去重；失败隔离、限速/重试/robots。

**抽取**
- 配置化指标词典 + 正则（`config/extract_rules.yaml`，14 类指标）。
- 正文预处理：清除脚注角标、`（GDP）` 标注、零宽字符；`(?<!人均)` 排除人均误匹配。
- 每条结果带 `raw_text` 原文溯源；预留 `LLMExtractor` 接口（二期）。

**分析与展示**
- CLI：`run.py`（采集）、`run.py analyze`（报告）。
- Web：FastAPI + 单页；运行历史/日志、机构与数据浏览、CSV 导出、分析报告预览。
- 分析：参考年度宽表、GDP 名义增速（需两年）、三次产业结构、每指标 CSV。

## 4. 验证证据（截至最近一次运行）

| 项 | 结果 |
|----|------|
| 自动化测试 | 57 passed（pytest，夹具驱动、离线可跑） |
| 真实端到端采集 | **13 机构（9 市全齐）/ 99 页 / 701 值 / 0 错误**（含漳州、南平、三明补全，多年 2020-2025） |
| 参考年度 | 2025 |
| 报告覆盖 | 10 地区（省 + 9 市）指标宽表；GDP 名义增速 6 市（多年度激活） |
| 数据自洽 | 9 地区三次产业和 = GDP、结构占比合计 100%，校验全 ✓ |
| 本地 git | 5+ commits（见 `git log --oneline`） |
| GitHub 仓库 | 已推送（含 README；commit 归属用户账号邮箱，计入 contribution） |

### 4.1 独立代码审查与修复（2026-09）

由独立审查子代理对 pipeline 健壮性与架构做只读审查，结论：**分层架构良好、依赖单向无环、注入式设计便于 fake；逐页/逐机构两层异常隔离正确**。已采纳修复：

| 级别 | 问题 | 处置 |
|------|------|------|
| C1 | run 状态机漏洞：全量失败也报 success，partial 分支不可达 | ✅ `_final_status`：errors==0→success / values>0→partial / 否则 failed（有回归测试） |
| C2 | URL 已存在内容更新时页面陈旧、数值重复累积 | ✅ upsert_page 刷新内容 + `replace_page_values` 整页替换（有回归测试） |
| I1 | robots.txt 每请求重抓、无超时 | ✅ 按站点缓存 + requests 带超时读取 + fail-open（有测试） |
| I2 | 发现阶段无隔离，单点异常整次 failed | ✅ 国家→省/省→市发现包 try，异常回退白名单锚点/记日志 |
| I3 | `Bureau.id` 回填契约未实现 | ✅ upsert 后回填 `.id`，resolver 直接用 parent.id |
| I4 | RuleExtractor 硬编码，LLM 无法注入 | ✅ `run_pipeline(..., extractor=None)` 可注入任意 Extractor |
| I6 | 候选 0 条无告警 | ✅ 记「未找到公报候选」日志 |
| I7 | 日志无 traceback | ✅ 页面/机构/管线异常均附 `traceback.format_exc()` |
| I8 | 两套解码口径不一致 | ✅ 统一 `fetch.parser.decode_html`（UTF-8→GB18030→GBK） |
| I5 | analysis 结果无稳定顺序 | ✅ `query_data` 加 `ORDER BY id` |
| Minor M1-M13 | 详见 §5 技术债 | 已采纳 M1 抖动退避等，其余记录待二期 |

合理取舍（审查认可）：同步串行采集、公报链接仅限本机域名（莆田 was 兜底）、规则抽取局限 + `raw_text` 溯源、reference_year 取全局最新、robots fail-open、手工锚点 verified=True。

### 4.2 「零数据」缺口治理（2026-09，A/B/C/D 四策）

真实验证发现两类缺口并针对性治理（均有回归测试）：

| 策 | 机制 | 解决 |
|----|------|------|
| **A 列表页下钻** | 公报候选若是栏目列表页（含同域「…统计公报…」详情链接）→ 自动下钻一层取详情正文，列表页本身不入库 | 三明等「首页→栏目页」结构此前被当正文抓取 → 0 值；现多年公报自动入库 |
| **B fallback 补漏** | 指标可配第二档宽松模式；章节限定 0 命中时全文再跑一次（护栏：城镇/农村排除、跨行容忍） | 常住人口 / CPI / 固定资产投资从全空到多市有值 |
| **C 自洽校验** | 分析层领域恒等式：三次产业和 = GDP、结构占比合计 ≈ 100% → ✓ / ⚠ 显性标注 | 噪声值显性化（如 730876 型假值会被否决） |
| **D listing_urls** | `manual_bureaus` 支持直连栏目 URL（漳州/南平 cms 站）；配合下钻取详情 | 漳州（GDP 6154）、南平（GDP 2190）等 cms 站补全 |

效果：13 机构 9 市全齐、99 页 / 701 值 / 0 错误；报告覆盖 10 地区；9 地区数据自洽全 ✓；名义增速激活（多年度 2020-2025）。

### 4.3 查询层宽容匹配（2026-09，M4.2）

用户反馈「查询福建省莆田市 2025 年第一产业增加值无结果」。诊断：**数据在库（158.35 亿元）而查询层为精确匹配**——`region=?` 精确对不上「福建省莆田市」/「2025年」。新增宽容解析（`repository.query_lax`，均有回归测试）：

| 输入 | 解析规则 | 例子 |
|------|---------|------|
| 地区 | 剥上级前缀（以地区名结尾）→ 后缀补全 → 双向包含；未识别给候选清单 | 「福建省莆田市」→莆田市；「莆田」→莆田市 |
| 指标 | 别名表（GDP→地区生产总值）→ 前缀补全 → 包含；歧义明确警告不静默 | 「第一产业」→第一产业增加值；「增加值」→ 警告多候选 |
| 年份 | 提取 4 位年份 | 「2025年」「2025 年度」→2025 |

关键取舍：**任一条件给出但解析失败时返回空行 + 明确警告**，绝不静默退回全量（避免用户误以为查询成功）。API 新增 `/api/filters` 供前端 datalist 候选提示（地区/指标/年份）；`/api/data` 与 `/api/export` 均走宽容解析。测试 36 → 47。

### 4.4 整句自然语言查询（2026-09，M4.3）

用户反馈「三个框还是呆板」。升级为 **`query_text` 整句解析**：一句话同时抽出地区/指标/年份（`/api/data?text=…`，Web 顶部主查询框回车即查，三框折叠为「高级筛选」）：

| 能力 | 规则 | 真实验证 |
|------|------|---------|
| 地区 | 候选全名或去省/市后缀核心名；**紧邻命中视为上级修饰下级**只留最具体（福建省莆田市→莆田市）；连接词分隔则并列保留（福州和厦门→两市）；无地区词→全省市 | 莆田/南平/泉州府(核心名补全)均 1 行命中 |
| 指标 | 全名优先 + 别名（GDP/第一产业…）；**「人均」前缀防误配**总量指标 | 人均地区生产总值→ 明确警告非收录口径 |
| 年份 | 多个 4 位年份取最后 + 警告 | 2024 和 2025 → 按 2025 |
| 防呆 | 地区有线索却未识别/指标未识别 → 空 + 候选清单；返回结构带 `matched`（已识别维度）供前端回显「已识别：地区…·指标…·年份…」 | 不存在市 → 0 行 + 候选清单 |

前端：主输入框 placeholder 即示例句；`matched` 回显让用户确认系统理解正确；导出按钮携带当前查询。测试 47 → 57。

### 4.5 泉州经济纵深画像（2026-09，M5）

用户指出「只有最抽象的截面，看不到支柱产业/核心机构/就业岗位分布/财税结构」。经澄清+侦察（5 类源全部实测 200 直连），落地**政府公开文件全家桶**单市样板（设计 `docs/DESIGN_M5_QUANZHOU.md`、计划 `docs/PLAN_M5_QUANZHOU.md`）：

| 维度 | 数据源 | 引擎 | 真实验证产出 |
|------|--------|------|------------|
| 财政税收结构 | 2025 统计公报 + 2026 预算执行 | 规则 | 收入 592.07 / 支出 880.29 / 基金 292.07 / 税收 814.34；自给率 67.26%、土地依赖 33.03% |
| 支柱产业图谱 | 十五五纲要(8.9 万字 PDF) 第三/六章 | LLM | 50 条产业洞察（含定位/规模/evidence） |
| 核心机构/市场主体 | 2025 上市后备企业名单(HTML 表格) | 表格 | **170 家**，县区 HHI 0.19（Top：晋江 53 家） |
| 就业岗位分布 | 统计公报 | 规则 | 城镇新增就业 9.49 万人 |
| 五年规划路径 | 政府工作报告「预期目标」段 | LLM | GDP 增速目标 5% |

**防数据美化（用户指定）**：`app/extract/critique.py` 双通道——规则校验 C1-C7（口径一致/三产恒等式/跨年突变/弹性异常/土地依赖/就业背离）+ LLM 批判审读（spin/omission/metric_game/gap，强制 evidence + confidence）。审读层只做「提示与质疑」不修改数据、不下「造假」结论。

**关键工程攻坚（真实验证暴露，均有回归测试）**：
- requests 走系统代理导致 LLM 大请求超时 → LLMClient 显式 `proxies=None` 直连（根因）
- 规划纲要 PDF 含目录，章节锚点误命中目录 → `start_marker` 跳目录 + `sections` 起止锚点对精准抽取（只送第三/六/十一章，政治性总论不抽）
- 复杂 schema + 长文本导致 qwen3.8-flash 生成慢 → 精简 schema、块 ≤2500 字、max_tokens 2000、超时 180s+重试
- 泉州画像串入福建省/漳州同名指标值 → 分析层全维度 `region='泉州市'` 隔离（回归测试 `test_region_isolation_no_cross_pollution`）
- 「政府性基金**预算**收入」表述 → 规则加「预算」可选

测试 57 → 127；泉州核心数据（GDP 13778.34 等）此前未入库，本轮公报源补齐 14 指标。报告 `data/reports/quanzhou_profile.md/html`（七章）。Web 新增「泉州画像」Tab（`/api/quanzhou/*`）。

### 4.6 经济驱动画像：产业结构错配 + 三驾马车（2026-09，M6）

用户指出「判断支柱产业不能只读政府规划，要有实际数据支撑」，并追加「CPI/收入中位数衡量消费支撑」「政府债务/GDP 衡量投资拉动、举一反三出口」。侦察确认可得：五经普 2023 分行业、统计年鉴（人口/收支/CPI/重点产业/进出口）、政府债务 2019-2024、决算支出；**制度性不可得**（地市不公布）：收入中位数、支出法 GDP、一般/专项债分项、分行业税收（设计 `docs/DESIGN_M6_INDUSTRY_GAP.md`、计划 `docs/PLAN_M6_INDUSTRY_GAP.md`）。

| 块 | 引擎 | 真实验证产出 |
|----|------|------------|
| 产业结构错配 | 五经普+年鉴 8-8+企业归类 | 规上工业重点产业 2016-2024 单位数/增速时序 295 行；规划「支柱/新兴」× 实际占比/增速/企业数 → 🔴规划强实际弱 / 🟢沉默支柱 / ⚪一致 |
| 消费支撑 | 年鉴 3-4 人口 / 4-7 收支 / 5-2 CPI / 公报社零 | 常住人口序列 146 行、可支配收入 52214、消费支出 33723、8 大类分项、恩格尔系数、CPI 历年；消费倾向/社零比 |
| 投资拉动 | 债务报告 2019-2024 | 债务余额/限额序列（2024：2661.16/2753.80 亿元）→ 债务/GDP 自算 |
| 净出口拉动 | 年鉴 12-3 | 出口/进口亿元序列（2024：1988/727）→ 外贸依存度 |
| 企业行业归类 | 12 类词典+LLM 兜底 | 170 家归类（电子信息 46、纺织鞋服 11…61 家待 LLM 兜底） |

**报告 Kami Parchment 风格（用户指定）**：`app/analysis/kami.py` KamiRenderer——暖羊皮纸底 `#f5f4ed`、墨蓝 `#1B365D` 唯一彩色、衬线字体（Noto Serif SC/Crimson Text）、数字等宽、标题 500 字重、墨蓝细线分节、章节目录锚点可跳转、🔴🟢 仅文字色。`economy_report.py` 组装五章画像 → `data/reports/quanzhou_economy.html`（`quanzhou.py --economy` / `/api/quanzhou/economy`）。

**关键工程攻坚（真实表结构驱动）**：
- 年鉴 HTML 表重度 colspan/rowspan → **lxml 自实现矩形展开器**（pandas 表头推断会吃表题行）；pandas/lxml 不落 requirements（系统 site-packages 提供）
- 每表专用解析：8-8 双列头（数据列序恒为 [企业数,增长%]，指标标签行因 colspan 错位不可信 → 固定列序赋名）、12-3 进出口（万美元/亿元双口径+子表头）、4-7 收支（行匹配）
- 债务正则锚定「债务余额预计执行数/中央核定的限额」，避免「新增限额 348.01」误配
- 测试 127 → 166；真实采集 series=383、industry_data=945、enterprises=170、errors=0

### §4.7 M7a 管线鲁棒性 P0(2026-09-08)

**触发**：全链路对抗性审查（`docs/REVIEW_PIPELINE_ROBUSTNESS.md`，第一性原理 + 真实库实测 + 双子代理专项）→ A/B 级发现：枚举式抽取重复入库、口径年份混装、空结果抹库、非事务替换、LLM 批判双层死代码、洞察无 region、硬编码 bureau_id、.doc 静默吞错。

**已交付**（设计 `docs/DESIGN_M7_ROBUSTNESS.md`、计划 `docs/PLAN_M7A_ROBUSTNESS.md`，TDD 逐任务红绿）：
| 项 | 变更 |
|---|---|
| 抽取单值化 | `rule_extractor`：`normalize_value`(全角/千分位/空格) + `_main_caliber_score` 主口径收敛 + %-占比误配排除 + 跨章节 `_converge_values` |
| 口径维度 | `data_values` +`caliber`(final/budget)/`source_url`；迁移幂等回填；**分析层默认只吃 final**(2026 预算行自动隔离) |
| 替换护栏 | 全部 `replace_*` 单事务+rollback；**空结果不删旧**(force 显式清空)；SQLite WAL/busy_timeout/外键 |
| 类型白名单 | `.doc/.xls` 等扩展名显式报错；PDF 文本<80 字判扫描版报错；bureau 归属按 region 查库/自建 meta（删除硬编码 id=1） |
| 批判双层接线 | `llm_critique` 每源落库 kind='critique'；SYSTEM_CRITIQUE +subject；`merge_checks` 同义归一 + 高置信单层升级；失败改上抛由调用方计数；`llm_errors/critiques` 统计 |
| region 隔离 | `doc_insights` +region/period；5 处分析调用点过滤（泉州画像/industry_gap 不再可能串染外市洞察） |
| 数据清理 | `scripts/cleanup_m7a.py`（备份 `stats.db.bak-20260908`，幂等）：取证删 9 行（宁德子口径/占比误配、三明断行残片、泉州海关代征税收）+ 完全重复 43 行；data_values 742→690；泉州 2026 三行 caliber=budget |

**验证**：测试 166 → **206 passed**（新增 40：迁移/事务/单值化/批判/region/清理）；真实库清理后抽查全部符合预期；API 冒烟 `/api/data?text=泉州税收收入2025` 仅返回 814.34(final)，2026 预算行带 caliber=budget。

## 5. 已知限制 / 技术债

| 类别 | 说明 | 影响 |
|------|------|------|
| 网络抖动 | 个别地市某次运行可能漏检 | 重跑幂等累积即可补回 |
| 存量页规则 | fallback 等新规则仅对新入库页生效（内容哈希跳过旧页） | 需清库重跑或引入强制刷新才更新存量 |
| 规则局限 | 个别公报排版极端（如三明 2025 GDP 段）仍可能漏抽 | fallback 已覆盖多数；`raw_text` 溯源 |
| 港澳台口径 | 公报注脚（金门/马祖等口径差异）未建模 | 数据含注脚说明，分析时注意 |
| 莆田 PDF | 若为扫描版 PDF 无法提取文字 | 需 OCR（未引入） |
| token 安全 | GitHub MCP token 明文在 `.mcp.json`、LLM key 在 `.env`（均 gitignore） | 建议定期轮换 |
| LLM 调用慢/抖动 | qwen3.8-flash 对长文本偶发生成慢；单市全量 ~10 分钟 | 已用 sections 精准切+2500 字块+超时重试缓解 |
| 泉州常住人口缺 | 泉州公报仅「年末户籍人口 773.49 万」，无常住人口口径 | 就业密度等派生指标为 None，不编造 |
| 收入中位数/支出法 GDP | 地市级制度性不公布 | 用消费倾向/城乡收入比/社零比代理，报告显式标注 |
| 企业归类 unknown | 规则词典未覆盖 61/170 家（如平台/贸易/设计类） | LLM 兜底（设计内，未接）；可按类扩充词典 |

## 6. 二期候选（M7，未排期）

1. `LLMExtractor` 深化：城镇/农村收入细分、批判审读对全 9 市推广、企业归类 LLM 兜底。
2. 增量刷新策略：新规则应用存量页（清库重跑或按 hash 变更强制刷新）。
3. 全国扩展（31 省 + 全部地级市；`manual_bureaus` 覆盖无独立域名站点）。
4. 采集/分析结果与报告的抽样质量校验工具化。
5. 泉州样板推广到福建其余 8 市（复用 quanzhou_sources.yaml 模式，抽取各市政府文件全家桶）。
6. Kami 报告风格推广到全省横比报告与 M5 画像（当前仅经济驱动画像使用）。

## 7. 快速接续（下次会话从这里开始）

```bash
cd E:\Deepseek_harness\stats-collector
.venv\Scripts\python -m pytest -q          # 确认 206 passed
.venv\Scripts\python run.py                 # 一键采集统计公报（网络抖动漏检可重跑）
.venv\Scripts\python run.py analyze         # 生成 data/reports/report.html（全省横比）
.venv\Scripts\python quanzhou.py            # 泉州纵深画像（采集+LLM+分析+报告）
.venv\Scripts\python quanzhou.py --economy  # M6 经济驱动画像（Kami 报告 data/reports/quanzhou_economy.html）
git log --oneline                           # 查看最近 commit
```

**规则提醒**：改代码走 TDD（先红后绿）；声称「完成/修复」前必须有新鲜验证证据；本文件随迭代更新。
